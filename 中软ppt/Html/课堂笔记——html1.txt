IE至少10以上兼容性还行
QQ,360浏览器只是在谷歌，火狐等浏览器的内核基础上套了一个自己的壳

<html>标记内容</html>
标记名

两个标记之间不能交叉，只能是包裹与被包裹的关系

<html></html>之间是页面内部
页面内部又分为两部分：1，头 2，体

<meta charset="utf-8"/>指定字符集

HBuilder  以Eclipse为基础
HBuilderX 由工作组自主研发

为什么要用HBuilder
1,有很好的提示
2,对移动端有很好的支持
3，国人开发，免费

html不区分大小写

RGB #000000黑色  #FFFFFF白色   相等的中间值 是灰色，接近0灰色越浅
可以简写：例，#CCC -> #CCCCCC

使用h1-h6  仅仅因为它只是个标题

<p>单词到达屏幕边缘也不会换行</p>

strong和em是html5新增的  代表强调
b和i没有语义  h5弃用

<sub></sub>下标
<sup></sup>上标    看圈在下边还是在上边

左中右  left center right
上中下  top middle bottom

alt  图片无法显示时，显示文本
img里图片没设置宽高，按原始大小显示
只设置宽或高，则图片根据原有宽高比例缩小或放大

GIF:优：支持透明，图片轮放  缺：最多支持256种颜色
JPEG:有很高的压缩比例，可被压缩  缺：有损压缩
PNG:画质好，支持透明，无损压缩，文件大

<p>默认情况下，多个空格只看成一个空格，<p>单词到达屏幕边缘也不会换行</p>

href后跟URL
绝对路径：格式：协议://ip:port/服务名/路径和资源名
	 访问网络上的资源

服务器/根目录路径:/服务名/路径和资源名
	        访问同一台电脑不同项目

相对路径：相对于当前文件的位置      访问同一个项目的资源
起点和目标有一方位置变了，就不管用了
../代表上一级

不要用盘符路径去访问资源


href 并没有拿过来，只是告诉你它在哪  是引用
src是把它复制一份，拿过来了  是引入

base：变更相对路径的起点，还可以设置链接统一的打开方式

图片放链接里，默认会有蓝边  ？
mailto:后跟收件人邮箱

<!--  -->注释

锚跳转  没有过渡，直接跳过去

dl 表示自定义列表整体的范围  dt标题   dd内容

table布局：冗余度高，不易于控制，要求每一列宽度必须相同

单元格在不合并的情况下永远不会出现边缘不整齐的情况

th默认居中   td默认居左

colspan 跨列  合并单元格  在一个行里操作
rowspan 跨行 合并单元格  在同一列里操作

表格的width  例：设置table的width=500;border=10;cellspacing=10;

       ||~~~td~~~td~~~||        双线为border    波浪线为cellspacing   那么这个表格总的宽度为：两个border的宽度+三个cellpacing的值30+两个单元格的边框共4px+500
				共554px    高度也一样

h5不再支持框架集了，取而代之的是内联框架
top代表最外层窗口(在iframe有多层嵌套的时候,会跳到最外层窗口)，parent代表父窗口    记住一定要用在超链接里才有用
