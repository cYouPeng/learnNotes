只有当目标字符串含有\n，而且正则表达式中含有^或$的时候，/m修饰符才有作用

不加g的话，找到第一个匹配项，就不会往下继续找了
m修饰符，多行匹配，不加m只匹配第一行，加m匹配多行
[]只代表一个字符，里边可以加^，表示除了什么什么，还有要表示所有大小写字母，要使用[A-z]，不能使用[a-Z]，否则会报错

let reg = /\d/g;
reg.test(str); test不是完全匹配才可以，只要有符合正则的子串就会返回true

关于量词的坑，let reg = /https?abc/;  此时量词的作用范围为前面的单个字符，要是想作用到整个https，需要把https加个小括号



正则对象.test(对比对象)返回true/false
search第一次出现的位置 有g也一样
全局匹配：match和split返回数组 replace返回字符串
let reg = /<[^<>]>/g; 防止贪婪原则