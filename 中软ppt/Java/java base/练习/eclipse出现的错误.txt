The declared package "" does not match the expected package "practice"
解决办法：添加一个包的声明
$ cannot be resolved or is not a field
解决办法，我把它放到引号外面了
Syntax error, insert ";" to complete LocalVariableDeclarationStatement
解决办法：replaceAll()的返回值我把它赋給了一个String类型的数组
The method join(CharSequence, CharSequence...) in the type String is not applicable for the arguments (char, String[])
解决办法：把单引号改为双引号
Type mismatch: cannot convert from String[] to String
解决办法：一开始我把split()赋给了一个String类型的变量
Syntax error on token "name", VariableDeclaratorId expected after this token
解决方法：我在类体中给对象的属性赋值
Type mismatch: cannot convert from int to int[]
解决办法：在二维数组没有指定一维和二维的情况下，在后面的大括号里我没有再写大括号，而是直接给值
Type mismatch: cannot convert from float to int
解决办法：把float类型直接转int，
Cannot define dimension expressions when an array initializer is provided
解决办法：我在给定数组长度的同时又指定了内容
break cannot be used outside of a loop or a switch
解决办法：我把break放到了一个单if语句里
The operator > is undefined for the argument type(s) String, int
解决办法：str.compareTo(otherStr>0)  //()>0
Syntax error on token "retnrn", return expected
解决方法：return拼错了
Type mismatch: cannot convert from int to boolean
解决方法：if(email.indexOf('.',atIndex))  indexOf返回int,我后边没给<0
The method split(String) in the type String is not applicable for the arguments (char)
解决办法：public String[] split(String regex)  里边我写成字符了
Implicit super constructor t() is undefined for default constructor. Must define an explicit constructor
解决办法：父类只有一个有参构造方法，实例化子类时，报错
The left-hand side of an assignment must be a variable
解决办法：对get()的返回值进行操作
The assignment to variable name has no effect   出现的是黄线
解决办法：属性和局部变量同名，我没有加this以区分
Illegal modifier for the enum constructor; only private is permitted.
解决办法：枚举类中不能有public构造方法
The left-hand side of an assignment must be a variable
解决办法：10/0  只写了这条语句
Unreachable code
解决办法：return后，我又加了条输出语句
Syntax error on token "public", interface expected after this token
解决方法：创建类时public后直接跟的类名