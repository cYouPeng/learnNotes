1、数据库
company
companyId	number	primary key,--有序列生成
companyName	varchar(50)	not null,
address		varchar(100)	not null,
tel		varchar(20)	not null,
memo		varchar(200)	not null

实现增删改查所有(编号，名，电话)查单个(所有的信息)

spring mvc 用注解方式
必须包含业务层
Dao层使用jdbc，BaseDao中使用properties文件配置数据库信息
要求使用自定义异常
命名必须符合规范