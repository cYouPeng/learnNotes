AJAX:利用js进行XML数据的传递
同步：发送请求，响应回来的数据是完整的html，之后浏览器进行渲染替换原来页面
<books>
  <book>
    <id></id>
    <name>雪山飞狐</name>     这是XML数据结构
  </book>
<books>  

js本身就支持json

get提交数据在RUL里
eval('('++')')  
option.disabled = true

相同：那5个步骤都是相同的
不同：绑定的函数体 eval前两个参数 send的参数