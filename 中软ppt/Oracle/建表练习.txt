学生表：tbl_student
stu_id	学号，主键，文本类型固定长度为6位
stu_name	姓名，非空，文本类型，不大于20位
stu_age	年龄，整数，必须大于0，非空，默认为20
stu_sex	性别，长度为不大于2字符，非空，值为‘男’或‘女’
address	地址，长度为不大于50字符，可以为空
tel	手机号，长度为11位字符，可以为空
email	电邮，长度为不超过50字符，可以为空

教师表：tbl_teacher
teac_id	教师编号，整数，主键
teac_name 教师名，长度不大于20字符，非空

科目表：tbl_course
cour_id		编号，整数,主键
cour_name
teac_id

成绩表：tbl_score
scor_id	成绩编号，主键，整数
stu_id	学号，学生表学号外键，非空，字符型
cour_id	科目编号，科目表编号外键，非空，整形
score	分数，可以为空，数字型，整数3位，小数1位，大于0


1写出完整的DDL（包括成绩，教师，科目分别对应的序列）
2，写出学生和教师的jdbc代码（包括增删改查）
